local nfpw = game.Players.LocalPlayer
local RoundTimer = nfpw.PlayerGui.ScreenGui.RoundTimer.Text

local function containsNumbers(str)
    return string.match(str, "%d") ~= nil
end

if containsNumbers(RoundTimer) then
    print("test1")
else
    print("test2")
end
